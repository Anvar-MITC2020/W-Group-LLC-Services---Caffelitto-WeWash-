# TODO

- [ ] Ввести раздельный выбор в заказе WeWash: сначала день, затем время.
- [ ] Обновить FSM: добавить states `choosing_day` и `choosing_time` (вместо текущего `choosing_time` напрямую).
- [ ] Обновить клавиатуры: создать `day_keyboard()` и `time_keyboard(day_index)`/`time_keyboard_for_day(date_str)`.
- [ ] Обновить обработчики `handlers/wewash.py`: 
  - переход от выбора extras к выбору дня
  - обработчик выбора дня → показ времени
  - обработчик выбора времени → запрос адреса (как сейчас)
- [ ] Обновить callback_data: 
  - день: `day:0|1`
  - время: `time:{YYYY-MM-DD HH:MM}`
- [ ] Прогнать линтер/запуск бота (если есть команда) для проверки синтаксиса.

